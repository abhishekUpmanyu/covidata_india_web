import 'package:covidata_web/utils/data.dart';
import 'package:flutter/material.dart';

class DistrictViewDesktop extends StatelessWidget {
  final DistrictData data;

  DistrictViewDesktop(this.data);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
